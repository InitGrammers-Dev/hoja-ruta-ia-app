{
  "name": "hoja-ruta-ia-app-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "infra"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "dev": "echo \"Ejecuta los scripts por paquete (apps/* o packages/*)\"",
    "build": "echo \"Ejecuta el build por paquete\"",
    "test": "echo \"Ejecuta los tests por paquete\""
  },
  "devDependencies": {
    "@types/pg": "^8.15.5"
  }
}
